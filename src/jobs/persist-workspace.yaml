description: |
  Reusable job for persisting a single provided path in a workspace.
parameters:
  path:
    description: "Path relative to working directory to persist."
    type: "string"
executor: architect
steps:
  - persist_to_workspace:
      root: .
      paths:
        - ./<< parameters.path >>
