parameters:
  setup-script:
    type: string
steps:
  - when:
      condition: << parameters.setup-script >>
      steps:
        - run:
            name: "Output test setup commands"
            command: |
              cat << parameters.setup-script >>
        - run:
            name: "Run test setup commands"
            command: |
              sh << parameters.setup-script >>
