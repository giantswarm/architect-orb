parameters:
  lockfile:
    type: "string"
    default: "go.sum"
steps:
  - save_cache:
      key: go-build-go-mod-v1-{{ checksum "<< parameters.lockfile >>" }}
      paths:
        - "/go/pkg/dep"
        - "/go/pkg/mod"
